// Platform-specific Agora bridge: Web / Server (Node SSR) mock implementation
// This avoids importing native react-native-agora which fails on non-native environments.
export const createRtcEngine = null;
export const ChannelProfileType = null;
export const ClientRoleType = null;
export const RtcSurfaceView = null;
export const RtcConnection = null;
